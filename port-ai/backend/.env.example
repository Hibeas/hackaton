# Supabase → Project Settings → Database → Connection string (URI)
# Use Session pooler (port 5432) for local uvicorn.
# Example:
# DATABASE_URL=postgresql://postgres.xxxxxxxxxxxx:YOUR_PASSWORD@aws-0-eu-central-1.pooler.supabase.com:5432/postgres
#
# Leave unset to use local SQLite (corridor_observations.db).
DATABASE_URL=

# TomTom — uncomment only with your own key (dev fallback exists in code).
# TOMTOM_API_KEY=

# Kafka — start broker: cd port-ai && docker compose up -d
KAFKA_BOOTSTRAP_SERVERS=localhost:8081
KAFKA_TOPIC=port-traffic-events
KAFKA_PUBLISH_ON_REFRESH=true
